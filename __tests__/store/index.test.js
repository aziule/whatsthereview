"use strict"

import store from '../../src/js/store/index'

describe('Main store', () => {
    it('must save the recorder when detected', () => {
        // TODO: implement tests for the store
        // const commit = jest.fn()
        // const recorder = {my: 'recorder'}
        //
        // store.actions.SPEECH_RECOGNITION_DETECTED({ commit }, { recorder })
        // expect(commit).toBeCalledWith(store.types.RECORDER_DETECTED, recorder)
    })
})